var express = require('express')
var router = require('router')

var app = express;

app.use('/', function(req, res){
     res.redirect(index.html)
});

app.Router('/admin', function(req, res){
    res.
})





var port = 3000;
const server = htt.createServer(app);
server.listen(port);

